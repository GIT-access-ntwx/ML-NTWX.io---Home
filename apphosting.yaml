kind: "AppHostingYaml"
version: "v1"
runConfig:
  basePath: "/"
env:
  - variable: API_KEY
    availability: [BUILD, RUNTIME]
# Ensure this is set in your Firebase Console settings for App Hosting
# Region: us-east4